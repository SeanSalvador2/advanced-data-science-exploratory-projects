{
  "name": "lecture-companion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/core",
    "cli",
    "app"
  ],
  "scripts": {
    "build": "npm run build --workspace @lecture/core && npm run build --workspace @lecture/cli && npm run build --workspace @lecture/app",
    "test": "npm run test --workspace @lecture/core && npm run test --workspace @lecture/cli && npm run test --workspace @lecture/app",
    "lint": "npm run lint --workspace @lecture/core && npm run lint --workspace @lecture/cli && npm run lint --workspace @lecture/app",
    "dry-run": "bash scripts/dry-run.sh"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "overrides": {
    "vite": "^6.4.3"
  }
}
